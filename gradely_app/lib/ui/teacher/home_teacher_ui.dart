import 'package:flutter/material.dart';

class HomeTeacherUI extends StatelessWidget {
  const HomeTeacherUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This is home teacher UI'),
    );
  }
}