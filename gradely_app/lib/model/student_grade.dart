class StudentGrade {
  final String uid;
  final String email;
  final String name;
  final int grade;
  final DateTime attendanceTime;

  StudentGrade(this.uid, this.email, this.name, this.grade, this.attendanceTime);
}